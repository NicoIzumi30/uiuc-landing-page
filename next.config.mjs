/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["ugm.ac.id","flagcdn.com"], // tambahkan domain di sini
  },
};

export default nextConfig;
